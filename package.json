{
  "name": "row-trader",
  "version": "1.0.0",
  "description": "Row-Trader - Safe Trading Platform for Kids",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "deploy-full": "bash scripts/deploy-full.sh",
    "db:init": "curl -X GET https://$(netlify status --json | grep -o '\"site_url\":\"[^\"]*' | cut -d'\"' -f4)/.netlify/functions/api-init-db || echo '⚠️  Site not deployed yet. Deploy first, then run: curl https://your-site.netlify.app/.netlify/functions/api-init-db'",
    "db:create": "netlify db:create",
    "netlify:dev": "netlify dev",
    "netlify:deploy": "npm run build && netlify deploy --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "netlify-identity-widget": "^1.9.2",
    "zustand": "^4.4.7"
  },
  "devDependencies": {
    "@netlify/functions": "^2.4.0",
    "@types/netlify-identity-widget": "^1.9.5",
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "vitest": "^1.1.0"
  }
}
